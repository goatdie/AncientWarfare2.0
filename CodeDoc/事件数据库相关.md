# 基础对象

Kingdom(ID, CURR_NAME, BANNER_ICON, BANNER_BG, START_TIME, END_TIME)

City(ID, CURR_NAME, START_TIME, END_TIME)

War(ID, CURR_NAME, START_TIME, END_TIME)

Alliance(ID, CURR_NAME, START_TIME, END_TIME)

# 事件

## 国家改名

KingdomChangeName(ID, TIMESTAMP, OLD_NAME)

## 国家改元

KingdomChangeYear(ID, TIMESTAMP, OLD_NAME)

## 城市改名

CityChangeName(ID, TIMESTAMP, OLD_NAME)

## 国王上下位

KingSet(KID, AID, TIMESTAMP)

## 国家参战

KingdomWar(KID, WID, TIMESTAMP, DEAD, KILL, EVENT_NAME)